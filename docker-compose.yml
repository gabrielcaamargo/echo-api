version: '3'

services:
    echo-api:
            image: postgres
            container_name: echo-api
            ports:
                - "5432:5432"
            environment:
                POSTGRES_USER: postgres
                POSTGRES_PASSWORD: postgres
                POSTGRES_DB: echo-api
